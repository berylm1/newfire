# Copy to .env-secrets on america at deploy time. NEVER commit.
DB_PASSWORD=replace_at_deploy_time
CF_ACCESS_AUD=will_be_filled_after_you_create_the_access_policy
# Set this ONLY during local development / smoke tests; leave EMPTY in prod.
OPENCLAW_DEV_EMAIL=
